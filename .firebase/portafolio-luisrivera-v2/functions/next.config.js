"use strict";

// next.config.js
var nextConfig = {
  images: {
    domains: ["i.postimg.cc"]
    // Agrega el dominio de tus imágenes aquí
  }
  // ... otras configuraciones de Next.js, si las tienes
};
module.exports = nextConfig;
